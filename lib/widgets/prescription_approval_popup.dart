import 'package:flutter/material.dart';


class PrescriptionPreview extends StatelessWidget {
  const PrescriptionPreview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
